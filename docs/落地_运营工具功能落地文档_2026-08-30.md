# 「运营工具」功能落地文档（2026-08-30）

> 配套需求文档：`docs/PRD_运营工具移植需求_2026-08-30.md`。
> **2026-08-30 用户裁决定稿**：飞书脚本创作**不移植**；分镜脚本创作、产品文案创作**划归媒体工具 Tab**。
> 「运营工具」范围 = 我的知识库 + 产品资料 + 视频评价预测 + 视频营销检测（4 项）。
> 本文档描述：目录/组件结构、路由、卡片清单、各工具落地路径与依赖核对、验收步骤。
> 铁律：单文件 ≤800 行；UI 层只绘制+事件转发；业务逻辑放 composables/services。

---

## 一、目录与组件结构

```
desktop/renderer/src/
├── views/OpsTools.vue                          # 运营工具 Launcher（卡片菜单 + 工具页容器）
├── views/MediaTools.vue                        # 媒体工具：增「产品文案创作」「分镜脚本创作」两卡
├── router/index.ts                             # 新增 /ops-tools 路由（media-tools 与 settings 之间）
├── App.vue                                     # tabs 数组中间插入「运营工具」
├── stores/app.ts                               # TabKey 增 'ops-tools'
└── components/ops-tools/
    ├── OtProductLibrary.vue                    # 产品资料（树状管理页）→ 运营工具
    ├── OtKnowledgeBase.vue                     # 我的知识库（风格化三区）→ 运营工具
    ├── OtVideoScore.vue                        # 视频评价预测 → 运营工具
    ├── OtVideoMarketing.vue                    # 视频营销检测 → 运营工具
    ├── OtProductCopywriting.vue                # 产品文案创作 → 媒体工具卡引用
    ├── OtStoryboard.vue                        # 分镜脚本创作 → 媒体工具卡引用
    └── OtToolCard.vue                          # 通用工具卡（图标+标题+说明，点击进入）
```

> 说明：划归媒体工具的两个组件仍放 `components/ops-tools/`（与知识库/产品资料共享 composables 与类型），由 MediaTools Launcher 的 `defineAsyncComponent` 引用；若后续需要彻底分域可再搬迁，不阻塞实现。

复用现有组件：
- 卡片 Launcher 模式：直接对齐 [MediaTools.vue](file:///d:/Project/TinTin_Client_Electron/desktop/renderer/src/views/MediaTools.vue)（`activeTool` + `backToGrid` + `defineAsyncComponent` 懒加载）
- 通用弹窗：`WbPickerDialog.vue`（产品/素材/脚本选择）
- 文件选择：`dialog.openFiles`（主进程 IPC）
- 服务端调用：`getTintin().server.get/post`（走 server-proxy，无 URL 拼装在组件内）
- 飞书凭证：读 `feishu.*`（设置页已存 electron-store，见 useSettingsAccounts）

---

## 二、路由与 Tab 注册

### 2.1 router/index.ts

在 `/media-tools` 前后插入：

```ts
{
  path: '/ops-tools',
  name: 'ops-tools',
  component: () => import('@/views/OpsTools.vue'),
  meta: { tab: 'ops-tools', title: '运营工具' }
},
```

### 2.2 App.vue tabs

```ts
const tabs: Array<{ key: TabKey; label: string; to: RouteLocationRaw }> = [
  { key: 'workbench',   label: '工作台',   to: '/workbench' },
  { key: 'ops-tools',   label: '运营工具', to: '/ops-tools' },
  { key: 'media-tools', label: '媒体工具', to: '/media-tools' }
]
```

### 2.3 stores/app.ts

`TabKey` 联合类型增 `'ops-tools'`；`switchTab` 的 C6 detachAll 逻辑自动覆盖（非 browser Tab 先 detach）。

---

## 三、OpsTools.vue Launcher 卡片清单（4 项，2026-08-30 定稿）

```ts
const GROUPS = ['方案脚本', '视频运营'] as const

const GROUP_TOOLS: Record<string, ToolItem[]> = {
  方案脚本: [
    { id: 'product-library',  title: '产品资料',     desc: '品类/品牌/型号树状管理，服务端同步', kind: 'comp', comp: OtProductLibrary },
    { id: 'knowledge-base',   title: '我的知识库',   desc: '风格化画像 + 参考素材蒸馏',         kind: 'comp', comp: OtKnowledgeBase },
  ],
  视频运营: [
    { id: 'video-score',      title: '视频评价预测', desc: '关键帧 → 视觉模型预测视频表现',      kind: 'comp', comp: OtVideoScore },
    { id: 'video-marketing',  title: '视频营销检测', desc: '研判是否营销视频 + 品类 + 改进建议',  kind: 'comp', comp: OtVideoMarketing },
  ],
}
```

**MediaTools.vue 增补**（划入自方案脚本组，进「创作」组）：

```ts
// 创作 组新增两张卡：
{ id: 'copywriting', title: '产品文案创作', desc: '选产品 → 风格化 → 一键生成文案', kind: 'comp', comp: defineAsyncComponent(() => import('@/components/ops-tools/OtProductCopywriting.vue')) },
{ id: 'storyboard',  title: '分镜脚本创作', desc: '文案 → 分镜 → 引用素材 → 保存脚本库', kind: 'comp', comp: defineAsyncComponent(() => import('@/components/ops-tools/OtStoryboard.vue')) },
```

---

## 四、各工具落地路径

### 4.1 产品资料（OtProductLibrary）

- 参照：product_library_page.py（树 + 搜索 + 表单归类）
- 服务端接口（已核实）：
  - `GET /api/product-library/search?q=&limit=` 检索
  - 同步/浏览 `GET /api/product-library`（同 manager 数据源）
- 落地要点：
  - 左树「品类 → 品牌 → 型号」、右表单（品类/备注/删除）
  - 删除走服务端 DELETE（按 manager 提供的接口，实现时以 openapi 核对）
  - 复制「to_prompt_text」摘要供文案创作（引用 ProductLibraryManager.to_prompt_text L382）

### 4.2 我的知识库（OtKnowledgeBase）

- 参照：my_knowledge_page.py 三区布局
- 数据层：MyKnowledgeManager（客户端本地 JSON/目录结构，需核对服务端是否有同步接口；原端是本地 KNOWLEDGE_MATERIALS_DIR）
- 落地要点：
  - 风格化分组列表（账号/内容类型/产品品类/行业垂类）→ 画像详情 → 素材列表
  - 工具栏：同步/导入/提炼
  - **待核对**：知识库是否迁移服务端（本地向量库 A2 已有 vss 表；原「我的知识库」为本地目录管理，R2 裁决后定）

### 4.3 产品文案创作（OtProductCopywriting）——媒体工具卡

- 参照：product_script_page.py
- 流程：产品选择（复用 fetchProducts）→ 展示性能/卖点（产品资料摘要）→ 可选风格化（知识库）→ 附加提示词 → 生成（LLM，走 `server.post('/llm/chat')` 或 agent 链路）→ 风格改写
- 落地要点：生成结果纯文本展示 + 复制；不落本地文件

### 4.4 分镜脚本创作（OtStoryboard）——媒体工具卡

- 参照：storyboard_page.py
- 服务端接口（已核实）：`GET /api/storyboard/scripts?topic=&page=&page_size=`（读）；保存脚本（写接口实现时以 openapi 核对）
- 流程：文案输入 → 生成分镜（每镜头：镜别/时长/音效/画面/旁白）→ 镜头引用素材（复用 fetchMaterials 语义搜索 + 即梦/联网素材）→ 保存（刷新工作台脚本列表）
- 落地要点：镜头列表竖向；引用素材复用 WbPickerDialog 骨架；即梦生成跳媒体工具素材生成（若已存在）或本地占位

> 原 4.4 飞书脚本创作（OtFeishuScript）已按 2026-08-30 裁决**移除，不移植**。

### 4.5 视频评价预测（OtVideoScore）

- 参照：hook_score_page.py
- 依赖：ffmpeg（已随包分发 resources/bin）+ 视觉模型
- 流程：选平台 → 上传视频（dialog.openFiles）→ 主进程 ffmpeg 抽关键帧 → 视觉模型预测（服务端 /llm/chat 带图或 /vision/*，实现时以 openapi 核对）→ 展示综合分+维度+建议 → 回填真实数据（本地持久化 video_prediction_manager 数据）
- 落地要点：ffmpeg 抽帧放主进程（montage 已示范 IPC）；预测结果展示卡

### 4.6 视频营销检测（OtVideoMarketing）

- 参照：marketing_detect_page.py
- 依赖：ffmpeg + 视觉模型（与 4.5 共用关键帧抽取与视觉调用）
- 流程：上传视频 → 抽关键帧 → 研判（是否营销/品类/特征/建议）→ 展示
- 落地要点：与 4.5 共用「视频 → 关键帧 → 视觉模型」composable，避免重复

---

## 五、逻辑解耦约定（铁律）

| 文件 | 职责 |
|---|---|
| `views/OpsTools.vue` | 容器：卡片清单 + activeTool 切换 + 事件转发，仅绘制 |
| `components/ops-tools/Ot*.vue` | 各工具展示组件：只消费 composable 状态 + emit 事件 |
| `composables/opsTools*.ts` | 各工具业务逻辑：fetch、IPC、状态计算（服务端调用经 getTintin().server） |
| `services/`（如需要） | 跨工具共享：关键帧抽取、视觉模型调用、飞书同步 |

每个 composable 对应一个工具的完整业务；组件不得内嵌 URL/IPC 调用。

---

## 六、验收步骤

1. **类型与门禁**：`npm run typecheck` exit 0；`node --check` 主进程改动；单测全过。
2. **路由/Tab**：顶栏出现「运营工具」且位于工作台与媒体工具之间；点击高亮正确；切换 Tab 无 BrowserView 残留（C6）。
3. **卡片菜单**：运营工具 4 张卡按两组展示；媒体工具「创作」组出现「产品文案创作」「分镜脚本创作」两卡；点击进入工具页，右上角返回卡片菜单。
4. **各工具主流程**（抽样）：
   - 产品资料：搜索「JBL」出树，单条归类/删除生效
   - 分镜脚本（媒体工具）：输入文案生成分镜，保存后工作台「选择脚本」能搜到
   - 产品文案（媒体工具）：选产品 → 生成文案 → 文案含产品卖点字段
   - 视频评价/营销（运营工具）：上传视频 → 抽帧 → 出评分/研判
5. **视觉验证**：浏览器打开 `/ops-tools` 与 `/media-tools` 逐卡检查布局与返回行为。

---

## 七、风险与待办

| 项 | 说明 |
|---|---|
| 知识库数据源 | 原端本地目录管理；新端是否走服务端（A2 本地向量已有 vss）需实现前核对 |
| 分镜/知识库写接口 | 读接口已核实；写/删除接口以服务端 openapi 为准 |
| 即梦素材/MG | 若新端素材生成尚未迁移，OtStoryboard 引用「即梦生成」先做占位入口 |
| 视觉模型调用 | 视频两工具共用视觉链路，先抽取 composable 再实现 UI |
| 飞书脚本创作 | **不移植**（2026-08-30 裁决）；若未来需要，另行立项 |
